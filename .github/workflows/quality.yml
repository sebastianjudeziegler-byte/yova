name: YOVA quality — release boundary red
on:
  pull_request:
  push:
    branches: [main]
concurrency:
  group: yova-quality-${{ github.ref }}
  cancel-in-progress: true
# Temporary red/green evidence only. Restore the full gate before merge.
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.16.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Verify mixed-criterion grader green
        run: |
          mkdir -p test-results/release-boundaries
          pnpm exec vitest run src/lib/openai/answer-evaluator.test.ts --maxWorkers=1 --retry=0 --reporter=default --reporter=json --outputFile=test-results/release-boundaries/grader-green.json
      - name: Establish equation, allocation and rubric red with negatives
        if: ${{ !cancelled() }}
        run: |
          result=0
          pnpm exec vitest run src/lib/session-generation/lesson-brief.test.ts src/lib/openai/streamed-validator-calibration.test.ts src/evals/brief-0-5-rubric.test.ts --maxWorkers=1 --retry=0 --reporter=default --reporter=json --outputFile=test-results/release-boundaries/validator-red.json || result=$?
          test "$result" -eq 1
          node -e 'const r=require("./test-results/release-boundaries/validator-red.json"); if(r.numFailedTests !== 7 || r.numPassedTests < 30) process.exit(1)'
      - name: Upload per-test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-boundaries-${{ github.run_id }}
          path: test-results/release-boundaries
          retention-days: 30
