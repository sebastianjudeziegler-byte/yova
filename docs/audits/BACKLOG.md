# Audit backlog

## Brief 0 — ranked REAL cases

These are unresolved defects or product-intent questions, not product fixes in this PR. There are 18 REAL cases: 17 from the original 36 plus one additional AP Biology case. A05/A15/A17 were initially STALE and were conservatively reclassified after corrected assertions exposed separate product failures or an intent question; the original three-run results are unchanged. Priority ranks learner impact, not distinct underlying causes. Commands use the ignored local provider configuration described in [the live-gate guide](../testing/live-gate.md).

### 1. A03 — admits uncertainty when the prompt lacks necessary context (P1)

Repro: run `YOVA_RUN_LIVE_ANSWER_EVALS=1 YOVA_ANSWER_EVAL_CASE=ambiguous-prompt node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/answer-evaluation-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. The rate-change answer was graded secure with no stated process, observations, or changed condition. Lifecycle exposure: Yes, at the answer-check step: an under-specified question receives a secure judgment instead of uncertainty, so the evidence-authority path could accept a judgment it cannot substantiate. Most likely cause: The evaluator trusts a vague reference/rubric instead of detecting missing observations and withholding judgment. [Per-test evidence](brief-0/EVIDENCE.md#a03).

### 2. X06 — builds a topic-specific teaching skeleton (P1)

Repro: run `YOVA_RUN_LIVE_STREAMED_EVALS=1 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/streamed-ap-biology.live.test.ts --maxWorkers=1 --testNamePattern 'builds\ a\ topic\-specific\ teaching\ skeleton'` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. The initial and compact recovery path ended in streamed_target_subject:other before a usable skeleton was returned. Lifecycle exposure: Yes, when opening a generated membrane-transport lesson after plan activation: generation rejects the skeleton and the lesson does not become runnable. Most likely cause: The target-to-subject validator rejects the provider’s bounded membrane/transport claims during finalization. [Per-test evidence](brief-0/EVIDENCE.md#x06).

### 3. A22 — Mapped product-rule and chain-rule first lesson (P1)

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=calculus_product_chain_streamed_25 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. SessionGenerationFailure: OpenAI did not return a complete streamed teaching skeleton after 1 repair attempt. The compact teaching recovery did not match its bounded content schema. Lifecycle exposure: Yes, at session generation or repair after activation: the learner receives a generation failure before the required lesson/check sequence can be completed. Most likely cause: The bounded compact-recovery output does not match the schema expected by session finalization. [Per-test evidence](brief-0/EVIDENCE.md#a22).

### 4. A15 — One product-rule skill in short sessions (P1)

Repro: run `YOVA_RUN_LIVE_JOURNEY_EVALS=1 YOVA_JOURNEY_EVAL_CASE=product_rule_narrow_15 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/plan-session-journey.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 2 assertion/semantic failures, 1 unavailable. Those baseline failures reached obsolete expectations; the independent failure or intent question below was exposed during branch verification. Session generation failed its bounded compact-recovery schema twice after the stale no-map assertion was corrected; telemetry says the formula-only active slice leaked the deferred factor-identification target. Lifecycle exposure: Yes, when opening the first product-rule lesson after activation: session generation cannot produce a runnable teaching/check sequence. Most likely cause: The authoritative slice separates the product-rule formula from identifying its factors, but generated teaching/checks cross that boundary and compact recovery does not satisfy the schema. [Per-test evidence](brief-0/EVIDENCE.md#a15).

### 5. A27 — Beginner JavaScript with fading support (P1)

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=javascript_scaffold_fading node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. SessionGenerationFailure: OpenAI did not return a complete streamed teaching skeleton after 1 repair attempt. The compact teaching recovery could not be finalized inside the authoritative session scope (streamed_target_assignment_duplicate:other). Stage: finalize. Lifecycle exposure: Yes, at session generation or repair after activation: the learner receives a generation failure before the required lesson/check sequence can be completed. Most likely cause: The provider/recovery assigns duplicate explanatory claims to the current slice, and deterministic finalization rejects them. [Per-test evidence](brief-0/EVIDENCE.md#a27).

### 6. A32 — Spanish conversation with supported transfer (P1)

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=spanish_conversation_transfer node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. SessionGenerationFailure: OpenAI did not return a complete streamed teaching skeleton after 1 repair attempt. The compact teaching recovery could not be finalized inside the authoritative session scope (streamed_target_assignment_duplicate:other). Stage: finalize. Lifecycle exposure: Yes, at session generation or repair after activation: the learner receives a generation failure before the required lesson/check sequence can be completed. Most likely cause: The provider/recovery assigns duplicate explanatory claims to the current slice, and deterministic finalization rejects them. [Per-test evidence](brief-0/EVIDENCE.md#a32).

### 7. A35 — delivers substantive teaching from the first generated lesson brief (P1)

Repro: run `YOVA_RUN_LIVE_WWI_LESSON_EVALS=1 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/streamed-world-war-one-lesson.live.test.ts --maxWorkers=1 --testNamePattern 'delivers\ substantive\ teaching\ from\ the\ first\ generated\ lesson\ brief'` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. SessionGenerationFailure: OpenAI did not return a complete streamed teaching skeleton after 1 repair attempt. The compact teaching recovery could not be finalized inside the authoritative session scope (streamed_target_subject:other). Stage: draft. Lifecycle exposure: Yes, at session generation or repair after activation: the learner receives a generation failure before the required lesson/check sequence can be completed. Most likely cause: The generated teaching claims fail the authoritative subject/scope validator; repair does not reliably recover that boundary. [Per-test evidence](brief-0/EVIDENCE.md#a35).

### 8. A36 — creates the production 45-minute streamed teaching session (P1)

Repro: run `YOVA_RUN_LIVE_WWI_BASELINE_EVALS=1 YOVA_RUN_LIVE_WWI_SKELETON_EVALS=1 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/streamed-world-war-one-skeleton.live.test.ts --maxWorkers=1 --testNamePattern 'creates\ the\ production\ 45\-minute\ streamed\ teaching\ session'` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. SessionGenerationFailure: OpenAI did not return a complete streamed teaching skeleton after 1 repair attempt. The compact teaching recovery could not be finalized inside the authoritative session scope (streamed_target_subject:other). Stage: draft. Lifecycle exposure: Yes, at session generation or repair after activation: the learner receives a generation failure before the required lesson/check sequence can be completed. Most likely cause: The generated teaching claims fail the authoritative subject/scope validator; repair does not reliably recover that boundary. [Per-test evidence](brief-0/EVIDENCE.md#a36).

### 9. A16 — World War I unit guide in short sessions (P1 (legacy material path))

Repro: run `YOVA_RUN_LIVE_JOURNEY_EVALS=1 YOVA_JOURNEY_EVAL_CASE=world_war_one_guide_15 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/plan-session-journey.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Generation refused the supplied material with “no readable explanatory source is mapped to the active target”; some attempts instead reached an obsolete rubric assertion. Lifecycle exposure: A tester can hit this when opening a plan with the legacy material-context shape in this fixture; this audit does not establish the same defect for every freshly mapped upload. Most likely cause: The retained legacy pure-material path reaches a fallback that requires mapped source authority; source-outline roles may also be inconsistent. The contract still promises legacy support, so this is a product-intent question, not permission to invent fixture mappings. [Per-test evidence](brief-0/EVIDENCE.md#a16).

### 10. A19 — Biology teaching from learner notes (P1 (legacy material path))

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=biology_initial_teaching node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Generation refused the supplied material with “no readable explanatory source is mapped to the active target”; some attempts instead reached an obsolete rubric assertion. Lifecycle exposure: A tester can hit this when opening a plan with the legacy material-context shape in this fixture; this audit does not establish the same defect for every freshly mapped upload. Most likely cause: The retained legacy pure-material path reaches a fallback that requires mapped source authority; source-outline roles may also be inconsistent. The contract still promises legacy support, so this is a product-intent question, not permission to invent fixture mappings. [Per-test evidence](brief-0/EVIDENCE.md#a19).

### 11. A24 — World War I teaching for a complete beginner (P1 (legacy material path))

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=world_war_one_novice_15_min node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Generation refused the supplied material with “no readable explanatory source is mapped to the active target”; some attempts instead reached an obsolete rubric assertion. Lifecycle exposure: A tester can hit this when opening a plan with the legacy material-context shape in this fixture; this audit does not establish the same defect for every freshly mapped upload. Most likely cause: The retained legacy pure-material path reaches a fallback that requires mapped source authority; source-outline roles may also be inconsistent. The contract still promises legacy support, so this is a product-intent question, not permission to invent fixture mappings. [Per-test evidence](brief-0/EVIDENCE.md#a24).

### 12. A30 — History reasoning from a primary-source excerpt (P1 (legacy material path))

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=history_source_reasoning node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Generation refused the supplied material with “no readable explanatory source is mapped to the active target”; some attempts instead reached an obsolete rubric assertion. Lifecycle exposure: A tester can hit this when opening a plan with the legacy material-context shape in this fixture; this audit does not establish the same defect for every freshly mapped upload. Most likely cause: The retained legacy pure-material path reaches a fallback that requires mapped source authority; source-outline roles may also be inconsistent. The contract still promises legacy support, so this is a product-intent question, not permission to invent fixture mappings. [Per-test evidence](brief-0/EVIDENCE.md#a30).

### 13. A31 — Literature close reading from a short passage (P1 (legacy material path))

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=literature_close_reading node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Generation refused the supplied material with “no readable explanatory source is mapped to the active target”; some attempts instead reached an obsolete rubric assertion. Lifecycle exposure: A tester can hit this when opening a plan with the legacy material-context shape in this fixture; this audit does not establish the same defect for every freshly mapped upload. Most likely cause: The retained legacy pure-material path reaches a fallback that requires mapped source authority; source-outline roles may also be inconsistent. The contract still promises legacy support, so this is a product-intent question, not permission to invent fixture mappings. [Per-test evidence](brief-0/EVIDENCE.md#a31).

### 14. A33 — Teaching from a thin biology study guide (P1 (legacy material path))

Repro: run `YOVA_RUN_LIVE_SESSION_EVALS=1 YOVA_SESSION_EVAL_CASE=thin_biology_outline_support node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/session-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Generation refused the supplied material with “no readable explanatory source is mapped to the active target”; some attempts instead reached an obsolete rubric assertion. Lifecycle exposure: A tester can hit this when opening a plan with the legacy material-context shape in this fixture; this audit does not establish the same defect for every freshly mapped upload. Most likely cause: The retained legacy pure-material path reaches a fallback that requires mapped source authority; source-outline roles may also be inconsistent. The contract still promises legacy support, so this is a product-intent question, not permission to invent fixture mappings. [Per-test evidence](brief-0/EVIDENCE.md#a33).

### 15. A05 — real placement questions and a map-only scope correction preserve demonstrated ATP (P2 (optional placement))

Repro: run `YOVA_RUN_LIVE_PLAN_BLOCKERS=1 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/plan-creation-blockers.live.test.ts --maxWorkers=1 --testNamePattern 'real placement questions'` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Those baseline failures reached obsolete expectations; the independent failure or intent question below was exposed during branch verification. The complete branch run rejected generated placement questions as ambiguous or unsupported before the corrected evidence/deferral assertion; a focused follow-up passed that assertion. Lifecycle exposure: Yes, during optional placement while creating a plan: the learner sees a generation error and must retry or skip placement. The flow without placement is not implicated by this observation. Most likely cause: The diagnostic generator and answer verifier sometimes disagree about answer support, consistent with the diagnostic failures seen in X03/X04. [Per-test evidence](brief-0/EVIDENCE.md#a05).

### 16. A17 — Full beginner calculus pathway (P2 (product-intent question))

Repro: run `YOVA_RUN_LIVE_JOURNEY_EVALS=1 YOVA_JOURNEY_EVAL_CASE=calculus_broad_pathway_30 node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/plan-session-journey.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. Those baseline failures reached obsolete expectations; the independent failure or intent question below was exposed during branch verification. The corrected connected journey generated a five-activity Worked Examples lesson scoring 85/100, then failed the required “Activities fit the learning task” check. No full learner activity dump was retained by that existing canary, so this is an unresolved intent question rather than a claim that the lesson was definitively wrong. Lifecycle exposure: The create/activate/open path can produce a runnable first lesson, but whether its activity mix meets the learner’s broad calculus goal is unresolved. This audit observed an eval failure, not a proven UI completion blocker. Most likely cause: The journey applies its plan-wide problem_solving task family to a foundational first Learn lesson; either the lexical test is too narrow or the lesson needs more explicit problem work. Founder decision required before changing that assertion. [Per-test evidence](brief-0/EVIDENCE.md#a17).

### 17. A01 — accepts a correct biology paraphrase (P2)

Repro: run `YOVA_RUN_LIVE_ANSWER_EVALS=1 YOVA_ANSWER_EVAL_CASE=biology-paraphrase node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/answer-evaluation-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. A correct paraphrase received secure plus non-empty missingIdeas even though those details are explicitly optional in the fixture rubric. Lifecycle exposure: Yes, during a typed-answer check: completion remains possible, but the learner sees a secure judgment beside a “What is still missing” list of optional details. Most likely cause: The verdict follows the required rubric while missingIdeas still pulls optional details from the reference answer. [Per-test evidence](brief-0/EVIDENCE.md#a01).

### 18. A02 — accepts a concise programming explanation (P2)

Repro: run `YOVA_RUN_LIVE_ANSWER_EVALS=1 YOVA_ANSWER_EVAL_CASE=programming-concise node --env-file=.env.local node_modules/vitest/vitest.mjs run src/evals/answer-evaluation-quality.live.test.ts --maxWorkers=1` with its saved fixture. Across three complete main runs: 0 passed, 3 assertion/semantic failures, 0 unavailable. A correct paraphrase received secure plus non-empty missingIdeas even though those details are explicitly optional in the fixture rubric. Lifecycle exposure: Yes, during a typed-answer check: completion remains possible, but the learner sees a secure judgment beside a “What is still missing” list of optional details. Most likely cause: The verdict follows the required rubric while missingIdeas still pulls optional details from the reference answer. [Per-test evidence](brief-0/EVIDENCE.md#a02).

## Unrelated ordinary-browser infrastructure observation

Repro: run all 284 ordinary desktop/mobile Playwright cases serially against one long-lived `next dev --webpack` server; the 61-minute verification run logged Next dev-server memory-threshold restarts and nine mobile timeouts/delayed UI transitions. Preserve the failed attempt and fresh-server replays in [Brief 0 evidence](brief-0/EVIDENCE.md#ordinary-regression-gates); no application code, browser assertion, timeout, or heap setting was changed, and no ordinary test was quarantined.

## Brief 0.5 — additional observation outside the requested fixes

- **Spanish recovery can lose the language (P2):** Repro A32 with the captured main run 1 context: the compact prompt repeats an English daily-routine objective without the goal's Spanish language, and the returned checks/references teach an English exchange. A tester can finish a session that does not practise Spanish. This predates Brief 0.5 and is separate from duplicate-claim rejection; no language-selection or product-shape change is made here. See `brief-0-5/evidence/main-live/run-1-session-quality-capture.jsonl`.


### Intermittent osmosis launch scope rejection (P1; Brief 0.5 comparison)

Repro: run the `osmosis` case in `src/evals/launch-session-journeys.live.test.ts` with `YOVA_RUN_LIVE_LAUNCH_EVALS=1` on main `e03a082`. In three fresh runs it passed twice and failed once because the generated claim assigned to target 2 did not preserve that target's subject terms. The first Brief 0.5 full gate also failed this case, with a missing active target, and its dependent lesson stream had no fixture. A real tester can hit this after choosing a 25-minute osmosis learning session and before opening the lesson. This case is outside Brief 0.5's ten-case scope; no osmosis-specific product fix or quarantine is included. Evidence: `docs/audits/brief-0-5/evidence/pass-to-fail-main/` and the first full-gate trial.


### Ordinary-browser comparisons during Brief 0.5 (outside scope)

- **Desktop cross-tab Calendar returns to Home (P2):** Run `e2e/calendar-journeys.spec.ts`'s “two simultaneous tabs preserve both new events and each tab refreshes” on desktop Chromium. The first tab returns to Home before Quick Add; the unchanged main `e03a082` comparison reproduces the same missing-input timeout as the initial branch run. This affects optional cross-tab Calendar use, not an established regression in the ten scoped live cases. No Calendar/storage fix is included. Evidence: `brief-0-5/evidence/browser-comparison/main/` and `brief-0-5/evidence/ordinary-browser/calendar-journeys/`.
- **Local misconception-repair journey waits past its browser timeout (P2; mechanism unresolved):** Run the desktop “a confident misconception is repaired now without a duplicate follow-up” case in `e2e/core-learning-loop.spec.ts`. The initial branch run waits for repair; unchanged main later waits for the formative evaluation and exhausts the overall 30-second test timeout, while mobile passes. Traces show pending local API responses, not a returned wrong learner judgment. Production impact is not established; no timeout/assertion or unrelated repair-runtime change is included. Evidence: `brief-0-5/evidence/browser-comparison/main/` and `brief-0-5/evidence/ordinary-browser/core-learning-loop/`.

- **Legacy Study Profile phone-width check (comparison pending CI):** Run the mobile “keeps every pre-report screen usable on common phone widths” case in `e2e/study-profile.spec.ts`; one local follow-up timed out waiting for the 14-question progressbar, with 18 other checks passing and one expected skip. The product/test path is unchanged; main comparison is deliberately deferred to CI at the founder's request. No profile fix or expectation change is included.

### Release audit — sharp dependency (authorized follow-up)

Repro: PR #84 CI run 34349726024, `pnpm audit --prod --audit-level high`, rejects `next > sharp 0.35.3` under GHSA-rgj7-g3m4-5g8c (high); quality gates after audit do not run. Founder explicitly authorized the minimal patched dependency update on 2026-09-09; sharp 0.35.4 and matching image binaries are prepared, CI confirmation pending. No product-scope expansion or local verification.
